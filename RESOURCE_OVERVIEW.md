# Resource Overview

## 전체 목표

보유 중인 클라우드 서버, ESP32-S3 기반 카메라 보드, 오디오 출력부, 모터 드라이버, 조향 서보, 구동 모터를 이용해 원격에서 영상 확인, 차량 주행 제어, 음성 전달, 상태 확인이 가능한 Wi-Fi RC카 시스템을 구성하려는 상황이다.

## 1. 클라우드/네트워크 자원

### Oracle Cloud VM

- 역할
  - 외부 인터넷에서 접속 가능한 중계 서버 자원
  - 브라우저 UI와 브로커 서버를 올리는 중심 노드
  - ESP32와 사용자 브라우저 사이를 연결하는 인터넷 측 허브
- 현재 확인된 스펙
  - `2 vCPU`
  - `약 1GB RAM`
  - `45GB 디스크`
  - Ubuntu 22.04 계열 Oracle 커널 환경
- 의미
  - 가벼운 웹 서버, WebSocket 브로커, 상태 중계에는 충분한 수준
  - 대규모 영상 트랜스코딩 서버가 아니라 중계와 제어 중심 자원
  - 브라우저 여러 개, ESP32 한 대 이상을 연결하는 구조의 핵심 서버 자원

## 2. 메인 제어 보드 자원

### Seeed XIAO ESP32-S3 Sense

- 역할
  - 전체 차량 시스템의 메인 컨트롤러
  - 카메라 캡처
  - Wi-Fi 연결
  - 오디오 입력/출력 연동
  - 모터 및 서보 제어
  - AP 설정 모드와 브로커 모드 전환의 중심 장치
- 이 보드가 제공하는 핵심 자원
  - ESP32-S3 MCU 연산 자원
  - 2.4GHz Wi-Fi 네트워크 자원
  - 카메라 인터페이스 자원
  - I2S/PDM 오디오 자원
  - PWM/GPIO 제어 자원
- 시스템 내 의미
  - 차량 쪽 모든 실제 하드웨어를 통합하는 단일 엣지 장치
  - 서버와 브라우저는 원격 인터페이스, 실제 센서/액추에이터는 이 보드에 집중

## 3. 카메라 자원

### XIAO ESP32-S3 Sense 내장 카메라

- 역할
  - 차량 전방 영상 획득
  - 원격 주행 시 시각 정보 제공
  - 스냅샷/스트리밍용 영상 소스
- 현재 사용 중인 카메라 핀 할당
  - `XCLK = GPIO10`
  - `SIOD = GPIO40`
  - `SIOC = GPIO39`
  - `D7 = GPIO48`
  - `D6 = GPIO11`
  - `D5 = GPIO12`
  - `D4 = GPIO14`
  - `D3 = GPIO16`
  - `D2 = GPIO18`
  - `D1 = GPIO17`
  - `D0 = GPIO15`
  - `VSYNC = GPIO38`
  - `HREF = GPIO47`
  - `PCLK = GPIO13`
  - `PWDN = -1`
  - `RESET = -1`
- 의미
  - 영상 스트리밍의 원천 자원
  - 카메라 달린 RC카를 가능하게 하는 핵심 센서 자원

## 4. 마이크 자원

### XIAO ESP32-S3 Sense 디지털 마이크

- 역할
  - 차량 주변 소리를 수집하는 입력 자원
  - 원격 모니터링 시 현장 음성 수집
- 현재 사용 중인 핀 할당
  - `MIC CLK = GPIO42`
  - `MIC DATA = GPIO41`
- 의미
  - 단순 영상 RC카가 아니라 현장 음성도 같이 듣는 장치로 확장시키는 자원
  - 차량 주위 소음, 대화, 환경음을 원격으로 확인하는 기반

## 5. 스피커/오디오 출력 자원

### MAX98357 / MAX98375 계열 I2S 앰프 모듈

- 역할
  - ESP32의 디지털 오디오를 실제 스피커 구동 신호로 바꾸는 출력부
  - 원격 음성을 차량 쪽에서 재생하는 자원
- 현재 사용 중인 핀 할당
  - `BCLK = GPIO7`
  - `WS = GPIO8`
  - `DIN = GPIO4`
- 의미
  - 차량이 단순 수신 장치가 아니라 원격 사용자의 음성을 현장에서 재생할 수 있게 하는 자원
  - 현장 알림, 음성 전달, 간단한 확성기 역할까지 가능하게 만드는 요소

## 6. 모터 드라이버 자원

### TB6612FNG

- 역할
  - ESP32의 저전력 제어 신호를 실제 DC 모터 구동 신호로 변환
  - 정회전/역회전/속도 제어를 담당하는 구동 인터페이스 자원
- 현재 사용 중인 핀 할당
  - `MOTOR PWM = GPIO2`
  - `DIR1 = GPIO5`
  - `DIR2 = GPIO6`
- 의미
  - 메인 보드와 실제 구동 모터 사이를 연결하는 필수 전력 제어 자원
  - 차량 추진력을 전자적으로 제어 가능하게 만드는 핵심 모듈

## 7. 구동 모터 자원

### DC 모터

- 역할
  - 차량을 실제로 전진/후진시키는 추진 자원
- 시스템 내 의미
  - 원격 제어 명령이 실제 움직임으로 바뀌는 최종 구동부
  - TB6612FNG와 함께 차량이 움직이는 기능 자체를 담당

## 8. 조향 자원

### 서보 모터

- 역할
  - 좌/우 조향을 담당하는 기계적 방향 제어 자원
- 현재 사용 중인 핀 할당
  - `SERVO PWM = GPIO3`
- 의미
  - 차량의 진행 방향을 세밀하게 바꾸는 핵심 액추에이터
  - 단순 직진 차량이 아니라 실제 조종 가능한 RC카로 만드는 자원

## 9. 상태 표시 및 보조 자원

### 상태 LED

- 핀
  - `STATUS LED = GPIO43`
- 역할
  - 현재 동작 상태, 연결 상태, 모드 상태 등을 표시하는 보조 자원

### 배터리 센싱

- 핀
  - `BATTERY SENSE = GPIO1`
- 역할
  - 차량 전원 상태를 읽는 모니터링 자원
  - 배터리 전압/잔량 추정의 기반

## 10. 소프트웨어 자원

### 브라우저 UI

- 역할
  - 사용자가 직접 조작하는 컨트롤 패널
  - 영상 보기, 조향, 음성, 상태 표시를 담당하는 프론트엔드 자원

### 브로커 서버

- 역할
  - ESP32와 브라우저 사이의 실시간 중계 자원
  - 제어, 상태, 영상, 음성 흐름을 인터넷 구간에서 이어주는 서버 자원

### 펌웨어

- 역할
  - 실제 하드웨어 자원을 제어하는 차량 내부 소프트웨어 자원
  - 카메라, 오디오, 모터, 네트워크를 통합 관리

## 자원 조합 관점에서의 정리

### 인터넷 측 자원

- Oracle VM
- 브로커 서버
- 웹 UI

### 차량 측 연산 자원

- XIAO ESP32-S3 Sense

### 차량 측 입력 자원

- 카메라
- 마이크
- 배터리 센싱

### 차량 측 출력/구동 자원

- MAX98357/MAX98375 계열 앰프
- 스피커
- TB6612FNG
- DC 모터
- 서보
- 상태 LED

## 이 자원들로 지향하는 시스템의 성격

- 원격 영상 확인이 가능한 이동체
- 원격 주행 제어가 가능한 차량
- 차량 주변 소리를 들을 수 있는 모니터링 장치
- 차량 쪽으로 음성을 보낼 수 있는 방송/호출 장치
- 클라우드 서버를 통해 외부 인터넷에서 접속 가능한 원격 로봇 플랫폼

## 한 줄 요약

보유 자원은 클라우드 중계 서버 1대, Wi-Fi 카메라/오디오/제어 보드 1개, 그리고 모터/조향/스피커 구동부로 구성되어 있으며, 이를 이용해 영상, 음성, 주행 제어가 가능한 인터넷 연결형 RC카/원격 이동 플랫폼을 만들려는 구성이다.
